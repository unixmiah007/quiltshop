# DB: if you kept 3307 in compose, use 3307; if you switched to 3306, use 3306.
DATABASE_URL="mysql://root:zrx185196!@127.0.0.1:3306/quiltshop"
MYSQL_URL="mysql://root:zrx185196!@127.0.0.1:3306/quiltshop"
# CORS / URLs
FRONTEND_URL="http://3.82.218.69"
# Or support multiple (dev + prod):
CORS_ORIGINS="http://3.82.218.69,http://localhost:4000, http://127.0.0.1:4000"

# Public URL used for image URLs returned by uploads and in seed.js:
BACKEND_URL="http://3.82.218.69"

# Backend server URL (only used in code for constructing links if needed)
BACKEND_URL_INTERNAL="http://127.0.0.1:4000"

PORT=4000

# JWT + Stripe
JWT_SECRET="change_me_to_a_long_random_string"
STRIPE_SECRET_KEY="sk_test_..."         # test key
# Optional webhook secret if you set one up:
STRIPE_WEBHOOK_SECRET=""
